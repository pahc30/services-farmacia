server:
  port: ${PORT:9000}

spring:
  application:
    name: appgw
  main:
    web-application-type: reactive
  cloud:
    compatibility-verifier:
      enabled: false
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials
      globalcors:
        add-to-simple-url-handler-mapping: true
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - http://localhost:4200
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true
      routes:
        - id: auth
          uri: http://farmacia-auth:7011
          predicates:
            - Path=/auth/**
        - id: usuario
          uri: http://farmacia-usuario:7012
          predicates:
            - Path=/usuario/**
        - id: producto
          uri: http://farmacia-producto:7013
          predicates:
            - Path=/producto/**
        - id: metodopago
          uri: http://farmacia-metodopago:7014
          predicates:
            - Path=/metodopago/**
        - id: compra
          uri: http://farmacia-compra:7015
          predicates:
            - Path=/compra/**

app:
  base-url: http://localhost:9000

# ConfiguraciÃ³n de logging para reducir warnings DNS en macOS
logging:
  level:
    "[io.netty.resolver.dns.DnsServerAddressStreamProviders]": WARN
    "[io.netty.resolver.dns.macos]": WARN

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway